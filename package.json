{
  "name": "@1024pix/ember-codemods",
  "version": "0.1.0",
  "scripts": {
    "test": "codemod-cli test",
    "update-docs": "codemod-cli update-docs",
    "deploy-patch": "npm version patch && git push && git push --tags && npm publish",
    "deploy-minor": "npm version minor && git push && git push --tags && npm publish",
    "lint": "eslint ."
  },
  "bin": "./bin/cli.js",
  "repository": "https://github.com/1024pix/ember-freshdesk-codemods",
  "license": "MIT",
  "keywords": [
    "codemod-cli"
  ],
  "dependencies": {
    "codemod-cli": "2.0.0"
  },
  "devDependencies": {
    "eslint": "^8.28.0",
    "eslint-config-prettier": "^8.5.0",
    "eslint-plugin-prettier": "^4.2.1",
    "jest": "^24.1.0",
    "prettier": "^2.7.1"
  },
  "jest": {
    "testEnvironment": "node"
  }
}
